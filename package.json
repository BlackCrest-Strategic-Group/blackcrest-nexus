{
  "name": "blackcrest-nexus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "npm run dev --prefix server",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && npm run prisma:generate --prefix server",
    "build:frontend": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && npm run prisma:generate --prefix server",
    "start": "npm run start --prefix server",
    "check:syntax": "node --check server/server.js"
  }
}
