import React from 'react';import {NavLink,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
const mods=[['intelligence','Intelligence'],['marketplace','Marketplace'],['capital','Capital'],['analytics','Analytics'],['sentinel','Sentinel'],['settings','Settings']];
export default function Layout({children}){const {user,logout}=useAuth();const nav=useNavigate();const signOut=()=>{logout();nav('/login')};return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">BC</div><div><strong>BlackCrest Nexus</strong><div className="muted" style={{fontSize:12}}>Command workspace</div></div></div><div className="eyebrow" style={{marginBottom:10}}>Workspace</div><nav className="nav-list">{mods.map(([path,label])=><NavLink key={path} to={'/'+path} className={({isActive})=>'nav-link'+(isActive?' active':'')}><span>{label}</span><span>›</span></NavLink>)}</nav></aside><main className="app-main"><header className="topbar"><div><div className="eyebrow">Operational overview</div><strong>Procurement command center</strong></div><div style={{display:'flex',alignItems:'center',gap:12}}><span className="status warn">3 alerts</span><span className="muted">{user?.email}</span><button className="btn btn-secondary" onClick={signOut}>Sign out</button></div></header><div className="page-content">{children}</div></main></div>}