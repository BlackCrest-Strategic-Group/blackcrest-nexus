export default function Settings(){const rows=[['Organization','BlackCrest Strategic Group'],['Deployment','Private Nexus environment'],['ERP connections','Configure connectors'],['Sentinel controls','Governance enabled']];return <div><div className="page-head"><div><div className="eyebrow">Workspace administration</div><h1 style={{margin:'.35rem 0'}}>Settings</h1><div className="muted">Manage the operating environment without changing existing credentials.</div></div></div><div className="data-list">{rows.map(([a,b])=><div key={a} className="panel data-row" style={{gridTemplateColumns:'1fr 2fr auto'}}><strong>{a}</strong><span className="muted">{b}</span><button className="btn btn-secondary">Manage</button></div>)}</div></div>}
