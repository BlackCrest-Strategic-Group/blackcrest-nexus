import React,{useState} from 'react';const tabs=['Industrial Materials','Logistics Capacity','Teaming & Subs'];const listings=Array.from({length:15}).map((_,i)=>({id:i+1,title:'Qualified source '+(i+1),supplier:'Supplier '+(i+1),location:['Southeast','Mid-Atlantic','National'][i%3],price:1000+i*100,cert:'ISO 9001'}));export default function Marketplace(){const [tab,setTab]=useState(tabs[0]);return <div><div className="page-head"><div><div className="eyebrow">Source network</div><h1 style={{margin:'.35rem 0'}}>Marketplace</h1><div className="muted">Discover qualified capacity and teaming options without losing governance.</div></div><button className="btn">Create requirement</button></div><div style={{display:'flex',gap:8,flexWrap:'wrap',marginBottom:18}}>{tabs.map(x=><button key={x} className={'btn '+(tab===x?'':'btn-secondary')} onClick={()=>setTab(x)}>{x}</button>)}</div><div className="data-list">{listings.map(l=><article key={l.id} className="panel data-row"><div><strong>{l.title}</strong><div className="muted" style={{fontSize:13}}>{l.supplier}</div></div><div><div className="muted">Coverage</div>{l.location}</div><div><div className="muted">Starting value</div>{'$'+l.price.toLocaleString()}</div><div><span className="status good">{l.cert}</span></div><div><button className="btn btn-secondary">Review</button></div></article>)}</div></div>}
